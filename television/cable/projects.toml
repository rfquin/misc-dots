[metadata]
name = "projects"
description = "A channel to select from your user's dotfiles"
requirements = ["fd", "bat"]

[source]
command = "fd -d 1 . $HOME/Dev/"

[preview]
command = "exa -T --color=always '{}'"

[keybindings]
shortcut = "f1"
